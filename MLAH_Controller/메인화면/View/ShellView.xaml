<dx:DXWindow x:Class="MLAH_Controller.ShellView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:local="clr-namespace:MLAH_Controller"
             mc:Ignorable="d"
             Title="시험운용통제 SW" Height="1440" Width="2560"
             WindowState="Maximized"
             dx:ThemeManager.ThemeName="Win11Dark"
            d:DataContext="{d:DesignInstance Type=local:ShellViewModel, IsDesignTimeCreatable=True}">

    <dx:DXWindow.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
    </dx:DXWindow.Resources>

    <Grid Background="{StaticResource MLAH_COLOR_BG_Brush}">

        <!--<local:View_ScenarioView Visibility="{Binding IsScenarioViewVisible, Converter={StaticResource BoolToVis}}"/>
        <local:View_MainView Visibility="{Binding IsMainViewVisible, Converter={StaticResource BoolToVis}}"/>-->

        <local:View_ScenarioView Visibility="{Binding IsScenarioViewVisible,UpdateSourceTrigger=PropertyChanged}"/>
        <local:View_MainView Visibility="{Binding IsMainViewVisible,UpdateSourceTrigger=PropertyChanged}"/>

        <!--<local:View_ScenarioView Visibility="{Binding IsScenarioViewVisible, Converter={StaticResource BoolToVis}}"/>-->

    </Grid>
</dx:DXWindow>